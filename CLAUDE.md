# Alonzo

Static site for alonzo.kr. Single-page `index.html` with inline CSS/JS; images live in the repo root and `mentors/`.

## Working style

- Never start the preview server and never open the browser preview.
- Never test or verify changes by running the site; just make the edit.
